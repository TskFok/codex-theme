# Task Plan: 生化危机里昂与艾达王 Codex Dream Skin 主题

## Goal

新增一套以里昂和艾达王为视觉核心、可离线导入 Codex Dream Skin 的生化危机主题，并沿用项目现有主题的资源契约、校验和打包结构。

## Current Phase

Phase 2：设计规格与实现计划

## Phases

### Phase 1：需求与项目结构发现

- [x] 读取现有主题、测试、校验器和导入脚本
- [x] 确认当前分支与工作区状态
- [x] 确认素材版权/生成方式与视觉范围
- [x] 确认主背景必须先由用户审阅，且支持按反馈重新生成
- **Status:** complete

### Phase 2：设计规格与实现计划

- [x] 提出 2–3 种主题视觉路线并确认推荐方案
- [x] 分段确认主题设计
- [x] 确认实现与验收流程
- [x] 编写并自检设计规格
- [x] 用户确认设计规格文件
- [x] 写出实现计划
- **Status:** complete

### Phase 3：主题实现

- [ ] 先写专项契约测试并确认红灯
- [ ] 生成、检查并整理主背景与侧栏纹理
- [ ] 创建 `theme.json` 与安全 `theme.css`
- [ ] 打包根目录四文件 ZIP
- **Status:** pending

### Phase 4：测试与验证

- [ ] 运行专项测试、主题校验器和 ZIP 完整性检查
- [ ] 运行全量测试并记录环境限制
- [ ] 检查变更边界与源目录/ZIP 字节一致性
- **Status:** pending

### Phase 5：交付

- [ ] 更新 README 或必要说明
- [ ] 完成工作区审查
- [ ] 向用户交付主题目录、ZIP 和验证结果
- **Status:** pending

## Key Questions

1. 主背景和侧栏纹理是否使用“原创灵感风格”的生成式插画/纹理，而不是直接复用游戏截图、官方 Logo 或官方素材？（已确认）
2. 主题是否保持项目现有的简化四文件结构，不修改安装器、引擎补丁和现有灭尽龙/初音未来主题？
3. 主背景是否需要在写入主题目录前由用户审阅，并可根据反馈重新生成？（已确认）

## Decisions Made

| Decision | Rationale |
|----------|-----------|
| 默认在当前 `master` 分支工作 | 用户约定未明确要求新建分支时不创建新分支 |
| 复用现有简化主题包契约 | 项目已有主题和 validator 已验证该结构，风险最低 |
| 主题重点放在深色警务/生化实验室氛围 | 能同时承载里昂的冷峻行动感与艾达的红色危险感 |
| 主背景采用“候选 → 用户审阅 → 反馈重生成 → 用户确认 → 写入主题目录”的门控流程 | 用户明确要求先审阅背景，不满意时允许重新生成 |
| 主背景设计采用 R.P.D. 夜巡 | 用户已确认推荐方案 A |
| 主题包命名与配色采用 `resident-evil-rpd` / R.P.D. Night Watch 契约 | 用户已确认设计第 2 部分 |
| 实施任务按测试、主背景审阅、侧栏纹理、配置、打包、全量验证拆分 | writing-plans 要求每个任务有独立输入/输出和测试门 |

## Errors Encountered

| Error | Attempt | Resolution |
|-------|---------|------------|
| agentmemory 没有可恢复的历史会话 | 1 | 以当前工作区和最近已提交的初音未来主题结构为上下文重新开始 |
| 规格自检扫描的 `???` 正则无效 | 1 | 改用固定字符串参数重新扫描，规格正文无错误 |
| 实施计划首次追加补丁缺少代码块行前缀 | 1 | 分段重新应用补丁，并对计划文件做结构和占位符扫描 |
| 实施计划分段补丁将 Task 3–6 插入到 Task 1–2 之前 | 1 | 按任务标记重新排序计划文件，并再次检查任务顺序 |
| 计划提交前 `git diff --cached --check` 报告文件末尾空行 | 1 | 删除末尾空行后重新暂存并检查 |

## Notes

- 工作区初始状态干净，当前分支为 `master`，HEAD 为 `3455ca6`。
- 最近完成的主题结构包括：主题目录四文件、根目录主题 ZIP、专项 `node:test`，图片尺寸契约为主背景 1920×1080、侧栏纹理 1024×1024。
- 任何新功能实现需遵循 TDD：先写会失败的专项测试，再写主题资源/实现。
- 实施计划已保存至 `docs/superpowers/plans/2026-08-11-resident-evil-rpd-dream-skin.md`；执行方式需在计划交接时选择。
