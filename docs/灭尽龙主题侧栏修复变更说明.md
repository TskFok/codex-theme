# 灭尽龙主题侧栏修复变更说明

## 主题素材包

- `assets/nergigante-dream-skin/background.png`：灭尽龙主背景图。
- `assets/nergigante-dream-skin/sidebar-pattern.png`：侧栏鳞刺暗纹。
- `assets/nergigante-dream-skin/decorative-spines.png`：透明装饰棘刺元素。
- `assets/nergigante-dream-skin/theme.json`：增加 `sidebarImage: "sidebar-pattern.png"`，并定义主题元数据与配色。
- `assets/nergigante-dream-skin/theme.css`：定义灭尽龙主题的根背景、文字和侧栏边框配色。
- `assets/Nergigante-Dream-Skin.zip`：可导入的主题包。

## 本机皮肤引擎：侧栏资源支持

- `/Users/ushopal/.codex/codex-dream-skin-studio/assets/theme-package-validator.mjs`
  - 允许主题包包含可选的 `sidebarImage`。
  - 验证文件名、格式、包内路径、文件内容及与主背景图的区别。

- `/Users/ushopal/.codex/codex-dream-skin-studio/scripts/extract-theme-zip-macos.sh`
  - 本地简化主题包允许三或四个文件。

- `/Users/ushopal/.codex/codex-dream-skin-studio/scripts/stage-theme.mjs`
  - 暂存并记录可选侧栏图片，纳入内容指纹。

- `/Users/ushopal/.codex/codex-dream-skin-studio/scripts/theme-content-fingerprint.mjs`
  - 内容指纹纳入侧栏图片字节。

- `/Users/ushopal/.codex/codex-dream-skin-studio/scripts/publish-theme-import.mjs`
  - 导入主题时保存和校验侧栏图片。

- `/Users/ushopal/.codex/codex-dream-skin-studio/scripts/injector.mjs`
  - 读取侧栏图片并传入渲染器注入模板。

- `/Users/ushopal/.codex/codex-dream-skin-studio/assets/renderer-inject.js`
  - 创建侧栏图片 Blob URL，并注入 `--dream-skin-sidebar-art` CSS 变量。

- `/Users/ushopal/.codex/codex-dream-skin-studio/scripts/switch-theme-macos.sh`
  - 切换主题时保留已暂存的侧栏图片，避免被清理逻辑删除。

## 侧栏显示问题修复

- `/Users/ushopal/.codex/codex-dream-skin-studio/assets/safe-css-validator.mjs`
  - 移除侧栏 `background-color` 自动追加 `background-image: none` 的行为。
  - 主内容与首页保持原有图片重置逻辑。

- `/Users/ushopal/.codex/codex-dream-skin-studio/assets/dream-skin.css`
  - 基础、`full`、`ambient` 及宽屏回退侧栏规则均保留 `sidebar-pattern.png` 图层。
  - 背景混合模式由 `normal, soft-light` 改为 `normal, normal`。
  - 深色 ambient 模式的侧栏遮罩从 `.70/.82` 降至 `.46/.58`，基础侧栏遮罩从 `.98/.96` 降至 `.72/.78`，使纹理可见且保留文字可读性。
  - 移除顶部 `::before` 主题名称/品牌标识，以及 `::after` 在线状态标识（包括 `SPIKES ONLINE`）。

## 回归测试

- `tests/sidebar-theme-support.test.mjs`：验证侧栏图片能安全打包并暂存。
- `tests/sidebar-background-color-regression.test.mjs`：验证侧栏底色不会清除侧栏图片。
- `tests/sidebar-task-mode-background.test.mjs`：验证 `full` 与 `ambient` 宽屏任务模式保留侧栏图片。
- `tests/sidebar-pattern-visibility.test.mjs`：验证所有侧栏图片规则采用普通叠加，且深色 ambient 模式使用可见度足够的遮罩值。
- `tests/theme-title-badge.test.mjs`：验证顶部不再生成主题名称、品牌或在线状态标识。

## 验证结果

- 8 项 Node.js 回归测试通过。
- 重新应用主题后，主 Codex 窗口的左侧栏已显示灭尽龙鳞刺纹理。
